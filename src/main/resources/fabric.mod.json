{
	"schemaVersion": 1,
	"id": "${id}",
	"name": "${display_name}",
	"version": "${version}",
	"description": "${description}",
	"authors": [
		"${author}"
	],
	"contact": {
		"sources": "https://github.com/${github}/${name}",
		"issues": "https://github.com/${github}/${name}/issues"
	},
	"license": "${license}",
	"icon": "assets/${namespace}/textures/mod_logo.png",
	"environment": "*",
	"mixins": [
		"mixins.${id}.json"
	],
	"entrypoints" : {
		"mixinsquared": [
			"${group}.${namespace}.LeafCullingMixinCanceller"
		]
	},
	"depends": {
		"minecraft": "${mc}",
		"fabricloader": ">=0.15.0",
		"fabric-api": "*"
	},
	"accessWidener": "${id}.accesswidener"
}